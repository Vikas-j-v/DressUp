<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DressUp - Organize Your Wardrobe Smartly</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />

    <style>
        .nav-link {
            transition: color 0.3s, background-color 0.3s;
        }
        .nav-link:hover {
            background-color: rgba(255, 255, 255, 0.2);
            color: #ffffff;
        }
        /* Full height body and html to allow footer to stick */
        html, body {
            height: 100%;
            margin: 0;
            display: flex;
            flex-direction: column;
        }
        /* Flex-grow for main content to push footer down */
        main {
            flex: 1;
        }
    </style>
</head>
<body class="bg-gray-100">

    <!-- Navbar -->
    <nav class="bg-gradient-to-r from-blue-600 to-blue-800 text-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <!-- Logo and Links -->
                <div class="flex items-center">
                    <!-- Logo -->
                    <div class="shrink-0">
                        <img class="h-10 w-auto" src="logo.png" alt="Logo">
                    </div>
                    <!-- Links (moved left) -->
                    <div class="ml-10 space-x-8 hidden sm:flex">
                        <a href="/" class="nav-link text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium">Home</a>
                        <a href="/about" class="nav-link text-gray-200 hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium">About Us</a>
                        <a href="/wardrobe" class="nav-link text-gray-200 hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium">YourCollections</a>
                        <a href="#" class="nav-link text-gray-200 hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium">Contacts</a>
                    </div>
                </div>

                <!-- User Actions -->
                <div class="hidden sm:flex sm:items-center">
                    <% if (user) { %> <!-- Check if user is logged in -->
                        <span class="text-gray-200 px-3 py-2 rounded-md text-sm font-medium">Hello, <%= user.username %></span>
                        <a href="/logout" class="bg-white text-blue-600 hover:bg-blue-500 hover:text-white border border-white px-4 py-2 rounded-md text-sm font-medium ml-4 transition">Logout</a>
                    <% } else { %> <!-- Show login/signup for guests -->
                        <a href="/login" class="bg-white text-blue-600 hover:bg-blue-500 hover:text-white border border-white px-4 py-2 rounded-md text-sm font-medium">Login</a>
                        <a href="/signup" class="ml-4 bg-blue-600 text-white hover:bg-blue-700 px-4 py-2 rounded-md text-sm font-medium">Sign Up</a>
                    <% } %>
                </div>
                


                <!-- Mobile menu button -->
                <div class="sm:hidden">
                    <button id="mobile-menu-button" type="button" class="text-gray-300 hover:text-white focus:outline-none">
                        <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="hidden sm:hidden">
            <div class="px-2 pt-2 pb-3 space-y-1">
                <a href="/" class="block text-white hover:bg-blue-700 px-3 py-2 rounded-md text-base font-medium">Home</a>
                <a href="/about" class="block text-gray-200 hover:bg-blue-700 px-3 py-2 rounded-md text-base font-medium">About Us</a>
                <a href="/wardrobe" class="block text-gray-200 hover:bg-blue-700 px-3 py-2 rounded-md text-base font-medium">Products</a>
                <a href="/contact" class="block text-gray-200 hover:bg-blue-700 px-3 py-2 rounded-md text-base font-medium">Contacts</a>
                <% if (user) { %> 
                    <a href="/logout" class="block bg-white text-blue-600 hover:bg-blue-500 hover:text-white px-3 py-2 rounded-md text-base font-medium">Logout</a>
                <% } else { %>
                    <a href="/login" class="block text-gray-200 hover:bg-blue-700 px-3 py-2 rounded-md text-base font-medium">Login</a>
                    <a href="/signup" class="block bg-white text-blue-600 hover:bg-blue-500 hover:text-white px-3 py-2 rounded-md text-base font-medium">Sign Up</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        <section class="bg-white py-20">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
                <h1 class="text-4xl font-bold text-blue-800">Welcome to DressUp</h1>
                <p class="mt-4 text-lg text-gray-700 max-w-3xl mx-auto">
                    Many individuals accumulate numerous clothes and accessories over time, making it increasingly difficult to manage their wardrobes. Often, they end up buying items they already own, while some of their favorite pieces remain unused and buried under the clutter. Additionally, there’s a growing concern about making more sustainable choices with personal possessions.
                </p>
                <p class="mt-4 text-lg text-gray-700 max-w-3xl mx-auto">
                    At <strong>DressUp</strong>, we help simplify wardrobe management, reduce overconsumption, and promote sustainable lifestyle choices. Whether you want to track how often you use items, plan outfits without the hassle, or discover better ways to care for your clothes, we’ve got you covered.
                </p>
                
                <% if (!user) { %>
                    <a href="/signup" class="mt-8 inline-block bg-blue-600 text-white px-6 py-3 rounded-md text-lg font-medium hover:bg-blue-700 transition">Get Started Today</a>
                <% } %>
        </section>

        <!-- Add Dress Section (only for logged-in users) -->
        <% if (user) { %>
        <section class="bg-gray-50 py-12">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <h2 class="text-3xl font-semibold text-gray-800 ">Bought Anything New</h2>
                
                <!-- Add a Dress Form -->
                <form action="/add-dress" method="POST" class="mt-8 flex">
                    <input type="text" name="dress" placeholder="Enter Dress Name" class="px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 w-full sm:w-auto">
                    <button type="submit" class="mt-2 sm:mt-0 sm:ml-4 bg-blue-600 text-white px-4 py-2 rounded-md">Add Dress</button>
                </form>

                <form action="/add-accessory" method="POST" class="mt-4">
                    <input type="text" name="accessory" placeholder="Add New Accessory" class="w-full sm:w-auto px-4 py-2 border border-gray-300 rounded-md">
                    <button type="submit" class="mt-2 sm:mt-0 sm:ml-4 bg-blue-600 text-white px-4 py-2 rounded-md">Add Accessory</button>
                </form>

                <form action="/add-item" method="POST" class="mt-4">
                    <input type="text" name="personalItem" placeholder="Add New Personal Item" class="w-full sm:w-auto px-4 py-2 border border-gray-300 rounded-md">
                    <button type="submit" class="mt-2 sm:mt-0 sm:ml-4 bg-blue-600 text-white px-4 py-2 rounded-md">Add Item</button>
                </form>
            </div>
        </section>
        <% } %>
    </main>

    <!-- Script for Dropdown -->
    <script>
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');

        mobileMenuButton.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
        });
    </script>
    
    <!-- Footer -->
    <footer class="bg-white shadow-md py-8 w-full">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center">
                <p class="text-gray-500">© 2024 DressUp. All rights reserved.</p>
                <div class="flex space-x-4">
                    <a href="#" class="text-gray-500 hover:text-blue-500">Privacy Policy</a>
                    <a href="#" class="text-gray-500 hover:text-blue-500">Terms of Service</a>
                </div>
            </div>
        </div>
    </footer>

</body>
</html>
